(
    symbol: "⑀",
    name: "Meta",
    description: "User-defined meta marker for arbitrary classification",
    sort_order: 100,
    category: "UserDefined",

    equivalence: ["status_marker", "arbitrary_classification", "flexible_tag"],
    definition: ["user-defined semantics ∧ no fixed meaning"],
    boundary: [
        "∂ user-defined (¬ERIS detection)",
        "⑀⊅{⟡,⋯,◈} (⑀ arbitrary | ⟡⋯◈ structured)",
    ],
    constraints: [
        "catch-all: workflow states | review flags | temporary markers",
    ],
    validation: ["⑀ ≡ user_defined ∧ arbitrary_semantics"],
)
